<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    >
    <gradient
        android:type="linear"
        android:angle="270"
        android:startColor="@color/white"
        android:centerColor="@color/top_background_color"
        android:endColor="@color/top_background_color" />

    <stroke
        android:width="2dp"
        android:color="@color/dark_accent" />

    <corners android:radius="12dp" />
</shape>