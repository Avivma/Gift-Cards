<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="com.example.composefirsttry.giftcard.logic.cards.model.GiftCardType" />
        <import type="com.example.composefirsttry.giftcard.utils.CardUtils" />
        <variable name="cardType" type="com.example.composefirsttry.giftcard.logic.cards.model.GiftCardType"/>
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="6dp"
        android:background="@drawable/card_details_background">

        <ImageView
            android:id="@+id/image"
            android:layout_width="84dp"
            android:layout_height="56dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginStart="12dp"
            tools:src="@drawable/max"
            app:loadImage="@{CardUtils.getCardImage(cardType)}"
            />

        <TextView
            android:id="@+id/name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="@+id/image"
            app:layout_constraintBottom_toBottomOf="@+id/image"
            app:layout_constraintStart_toEndOf="@id/image"
            android:layout_marginStart="12dp"
            android:textSize="@dimen/small_main_text_size"
            android:textColor="@color/dark_accent"
            android:text="@{CardUtils.getCardName(cardType)}"
            tools:text="Max"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
