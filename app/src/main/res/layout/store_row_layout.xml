<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" >

    <data>
        <import type="android.view.View" />
        <variable name="storeName" type="java.lang.String"/>
        <variable name="storeSelected" type="java.lang.Boolean"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp">

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="@id/store_name_tv"
            android:background="@drawable/store_cards_frame"
            />

        <TextView
            android:id="@+id/store_name_tv"
            android:layout_width="128dp"
            android:layout_height="72dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:textColor="@{storeSelected ? @color/selected_store_text_color : @color/store_text_color, default=@color/store_text_color}"
            android:gravity="center"
            android:textSize="17sp"
            android:textStyle="bold"
            android:background="@drawable/store_frame"
            android:text="@{storeName}"
            tools:text="Mega Sport"
            />

        <androidx.constraintlayout.widget.Guideline
            android:id="@+id/guideline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="@id/store_name_tv"
            android:orientation="horizontal"
            app:layout_constraintGuide_percent="0.24"
            />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_view"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="6dp"
            app:layout_constraintTop_toTopOf="@id/guideline"
            app:layout_constraintStart_toEndOf="@id/store_name_tv"
            app:layout_constraintEnd_toEndOf="parent"
            android:orientation="horizontal"
            tools:itemCount="3"
            tools:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/gift_card_small_layout"
            tools:orientation="horizontal"
            tools:visibility="visible"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
