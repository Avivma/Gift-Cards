<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable name="item" type="com.example.composefirsttry.giftcard.ui.common.dialog.advancedialog.CustomDialogAdapterItem.Selectable"/>
        <variable name="imageRes" type="java.lang.Float" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/row_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="6dp"
        android:background="@drawable/card_details_background"
        >

        <View
            android:id="@+id/frame"
            android:layout_width="0dp"
            android:layout_height="64dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginStart="6dp"
            android:background="@drawable/store_frame"
            />

        <com.google.android.material.checkbox.MaterialCheckBox
            android:id="@+id/check_box"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            app:layout_constraintTop_toTopOf="@id/frame"
            app:layout_constraintBottom_toBottomOf="@id/frame"
            app:layout_constraintStart_toStartOf="parent"
            android:checked="@{item.selected}"
            tools:checked="true"
            />

        <ImageView
            android:id="@+id/image"
            android:layout_width="84dp"
            android:layout_height="56dp"
            app:layout_constraintTop_toTopOf="@id/frame"
            app:layout_constraintBottom_toBottomOf="@id/frame"
            app:layout_constraintStart_toEndOf="@id/check_box"
            android:alpha="@{item.selected ? 1.0f : 0.5f, default=1.0f}"
            app:loadImage="@{item.imageUrl}"
            tools:src="@drawable/max"
            />

        <TextView
            android:id="@+id/name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="@+id/image"
            app:layout_constraintBottom_toBottomOf="@+id/image"
            app:layout_constraintStart_toEndOf="@id/image"
            android:layout_marginStart="12dp"
            android:textSize="@dimen/small_main_text_size"
            android:textColor="@color/dark_accent"
            android:text="@{item.title}"
            tools:text="Max"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
